# ORCA backend — deliberately small so it installs in seconds on any laptop.
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.6
httpx>=0.27
pyyaml>=6.0
langgraph>=0.0.1

# LLM rephrase layer (Groq -> fast open-source inference).
# Install:  pip install groq
# Activate: set GROQ_API_KEY=gsk_...
groq>=0.9

# Scheduling (background data refresh jobs)
apscheduler>=3.10

# Web scraping (IMD, INCOIS advisory scrapers)
beautifulsoup4>=4.12

# MOSDAC satellite data parsing (NetCDF3/HDF5/NetCDF4 files from ISRO MOSDAC)
# E06OCM_L4_AC files are NetCDF3 classic (CDF1 magic). Requires scipy.
h5py>=3.9          # HDF5 / NetCDF4 format support
numpy>=1.26        # array math (shared dependency)
scipy>=1.12        # NetCDF3 classic format (scipy.io.netcdf_file)

# Optional (production / advanced ML path — the prototype runs without them):
# scikit-learn>=1.4      # risk model training
# xgboost>=2.0           # gradient-boosted risk + PFZ ranking
# shapely>=2.0           # swap in for the pure-python geometry in data/geo.py
# psycopg[binary]>=3.1   # PostgreSQL + PostGIS
